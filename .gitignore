*.pem
pair_*.png

__pycache__
dist
build
.venv
.venv-win

*pb2_grpc.py
*pb2.py

# remote/ 的 gRPC 存根是**运行必需**, 不是构建产物 (proto 在 remote/proto/kuuki_remote.proto,
# 重新生成用 `bash remote/proto/gen_proto.sh`), 所以允许入库。
!remote/proto/*.py

# ---- 演示/测试产物不入库 ----
play/
_clicks.jsonl
_events.jsonl

# ---- WorkBuddy 工作目录 (会话记忆 / 项目数据) 不入库 ----
.workbuddy/

# ---- 早期原型: 2025-09 的手机虚拟键盘单页, 与现在的 remote/ 无引用关系 ----
# 文件本身保留在磁盘上, 只是不入库 (否则 git status 永远挂着一行未跟踪)
legacy/
