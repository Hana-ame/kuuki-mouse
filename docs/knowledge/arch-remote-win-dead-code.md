# `remote/win/` 是实验性死代码

`remote/win/`（`host.py` + `winhost.ps1`）是一套 WSL→Windows 桥。

**关键事实：全仓库没有任何地方 import 它**（grep 过所有 `*.py`，只有注释和文档提到
它）。所以：

- 改服务端逻辑时**不用管它**，不会有架构冲突
- 「保留」= 不动它，也不需要维护它
- 文档里已标注它是实验性、未接入服务端

别把它当成「Windows 原生实现的一部分」去参照 —— 它的命名很容易误导。
