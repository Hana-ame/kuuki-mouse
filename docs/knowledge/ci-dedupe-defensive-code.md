# 同一段防御代码抄到第三份时，就该抽出来

英文 CI 上连续炸了 4 次「中文编码」错误，前三次**各修各的**（每个文件里贴一份
`_force_utf8_stdio`），到第四次才抽出 `utf8_stdio.py` 作为唯一实现，四处共用。

教训很直白：**复制第二份是省事，第三份就该停手。** 判断标准不是「这段多长」，而是
「它是同一件事的同一个正确写法吗」—— 同一个 bug 修了三遍还在第四处复现，说明真正
的修复一直没做。

`utf8_stdio.py` 现在是唯一实现，新增任何会输出中文的 CI 脚本都直接 import 它。
