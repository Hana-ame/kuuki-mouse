# 真人正在用这台电脑时，注入会互相干扰

自动化过程中的现象：莫名其妙多出标签页、点了没反应、焦点乱跳。**很可能对方此刻也在
动鼠标键盘** —— 两路输入混在同一台机器上会互相打断，而且分不清哪个结果是谁造成的。

应对：

- **每一步都靠截图确认，不信返回值**：op 返回成功只代表服务端执行了注入，不代表
  前台窗口拿到了它
- 关键动作（发送、提交）之前再截一次图确认当前焦点
- 演示性操作用「零位移」版本（`move-rel --dx 0 --dy 0`），别真搬别人的光标

这是全自动 GUI 操作的真实约束，不是 bug。
