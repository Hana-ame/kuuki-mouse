# kuuki-mouse 远程控制扩展的额外依赖 (WebSocket + gRPC + 截屏)
#
# 与 requirements.txt 的关系:
#   - requirements.txt 里的 pynput / Pillow 已经覆盖了输入控制与图片编码, 不用重复装
#   - websockets 原本只是 peerjs fork 的隐式依赖, 这里显式写出来
#
# 安装: pip install -r requirements.txt -r requirements-remote.txt
#
# 版本下限的依据 (2026-09-20):
#   - protobuf: remote/proto/ 下的存根由 protoc **7.35.1** 生成, 运行时低于它会在
#     import 时被 runtime_version 校验直接拒绝。改 proto 重新生成后要同步这个下限。
#   - grpcio: 生成的存根用了 `_registered_method` 参数 (grpcio 1.63 起才有)。
#   - 截屏只有一个后端 **PIL.ImageGrab** (由 requirements.txt 的 Pillow 提供);
#     早先文档里写的 mss / ffmpeg x11grab 降级链已不存在, 相应依赖也不再需要。
#   - grpcio-tools 只在重新生成 proto 存根时才要 (bash remote/proto/gen_proto.sh)。

websockets>=13
grpcio>=1.63
protobuf>=7.35

# 可选: 只在改 proto 后重新生成存根时需要 (与存根的生成版本保持一致)
# grpcio-tools>=1.84
